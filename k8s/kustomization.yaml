apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: futu-opend
resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
# secret.example.yaml is intentionally NOT listed here — credentials must be
# created out-of-band (see README.md and secret.example.yaml).
