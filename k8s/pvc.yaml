apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: futu-opend-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # storageClassName intentionally omitted so the cluster default is used
  # (kind ships local-path-provisioner as `standard`).
