# Reference template only — DO NOT commit a real version of this file.
#
# Prefer creating the secret out-of-band so the RSA key never lives in YAML:
#
#   kubectl create namespace futu-opend
#   kubectl create secret generic futu-credentials \
#     --namespace futu-opend \
#     --from-file=futu.pem=./futu.pem \
#     --from-literal=FUTU_ACCOUNT_ID=<your-id> \
#     --from-literal=FUTU_ACCOUNT_PWD=<your-password>
#
# Use FUTU_ACCOUNT_PWD_MD5 in place of FUTU_ACCOUNT_PWD if you'd rather not
# expose the plaintext password to the container (start.sh hashes plaintext at
# runtime if MD5 is unset).
---
apiVersion: v1
kind: Secret
metadata:
  name: futu-credentials
  namespace: futu-opend
type: Opaque
stringData:
  FUTU_ACCOUNT_ID: "REPLACE_ME"
  FUTU_ACCOUNT_PWD: "REPLACE_ME"
  # FUTU_ACCOUNT_PWD_MD5: "REPLACE_ME"
  futu.pem: |
    -----BEGIN RSA PRIVATE KEY-----
    REPLACE_ME...
    -----END RSA PRIVATE KEY-----
